// const colors = require('colors');
// console.log("hello".bgBlue)

//if npm package deleted by anyone mistakenly then we should run npm install command this command will follow dependciency in package.json folder and 
//run install all depenciedy which is declared

//how to ignore node_modules file while pushing code on github : we have to create .gitignore file and write the name of folder or file which we dont 
//want to push on git hub

